
// Maybe we will actually write this code?


let leftFoot;
let rightFoot;


// right foot forward
// left foot forward/
// right foot forward
// left foot forward
// right foot forward
// left foot forward

// open door

// turn L

function setup () {
  // runs once at beginning
}

function draw() {

  // loop over and over again

  step(6);
  turnLeft();
  step(4);
  turnLeft();
  step(5);
  turnRight();
  step(5);
  turnLeft();
  step(2);

}


function step(numberSteps) {
    for (var i = 0; i < numberSteps; i++) {
      // right foot forward
      // left foot forward
    }
}


function turnLeft() {
  // turn L
}

function turnRight() {
  // turn L
}
